export default function StatCard({ label, value, description, icon, tone = "bg-rose-50 text-[#7A0E14]", onClick }) { const Tag = onClick ? "button" : "article"; return <Tag onClick={onClick} className={`w-full rounded-2xl border border-slate-200 bg-white p-5 text-left shadow-[0_8px_24px_rgba(15,23,42,.06)] transition ${onClick ? "hover:-translate-y-0.5 hover:shadow-md" : ""}`}><div className="flex items-start justify-between gap-3"><div><p className="text-sm font-medium text-slate-500">{label}</p><p className="mt-2 text-2xl font-bold text-slate-900">{value}</p></div>{icon && <span className={`grid h-11 w-11 place-items-center rounded-xl ${tone}`}>{icon}</span>}</div>{description && <p className="mt-3 text-xs font-medium text-slate-500">{description}</p>}</Tag>; }
