export default function FilterBar({ filters = [], className = "" }) { return <div className={`flex flex-wrap gap-3 ${className}`}>{filters.map(({ label, value, onChange, options }) => <label key={label} className="text-sm font-medium text-slate-600"><span className="sr-only">{label}</span><select value={value} onChange={onChange} className="rounded-xl border border-slate-200 bg-white px-3 py-2.5 text-sm outline-none focus:border-[#7A0E14]"><option value="">{label}</option>{options.map((option) => <option key={option.value ?? option} value={option.value ?? option}>{option.label ?? option}</option>)}</select></label>)}</div>; }
